html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    title.
      'Vue.js boilerplate'
  
  body
    div#app
      h1= message
  
  script(src='https://unpkg.com/vue@2.0.3/dist/vue.js')
  
  script.
    var app = new Vue({
      el: '#app',
      data: {
        message: "I printed this using Vue.js!"
      }
    });
